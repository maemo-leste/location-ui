AUTOMAKE_OPTIONS = foreign
ACLOCAL_AMFLAGS  = -I m4

SUBDIRS = src

servicesdir = $(datadir)/dbus-1/system-services
services_DATA = com.nokia.Location.UI.service

#com.nokia.Location.UI.service:
#	sed -e "s,@LIBDIR@,$(libdir)," < $@.in > $@
